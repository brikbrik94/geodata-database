# geodata-database

Central repository for GeoJSON to PostgreSQL imports for the `emergency_db`.

## Setup
Run `./init.sh` to initialize the environment and submodules.

## Usage
Run scripts via the virtual environment:
`source .venv/bin/activate`
`python scripts/import_nah.py`
